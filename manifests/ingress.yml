apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: "cluster01-ff64"
    labels:
        app: "cluster01-ff64"
    annotations:
        kubernetes.io/ingress.class: azure/application-gateway

spec:
    rules:
        http:
            paths:
            - path: /
                backend:
                    service:
                        name: "cluster01-ff64"
                        port:
                            number: 80
              pathType: Exact
