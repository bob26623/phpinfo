apiVersion: v1
kind: Service
metadata:
    name: "cluster01-ff64"
    labels:
        app: "cluster01-ff64"
spec:
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "cluster01-ff64"
